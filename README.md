# Food Order App

Food Order is a full-stack web application that allows users to browse and order food from restaurants. The app has two main parts:

- **Backend**: ASP.NET Core Web API (`food-order-api`) providing REST endpoints for managing restaurants, dishes, and orders.
- **Frontend**: Angular SPA (`frontend`) for users to view menus and place orders via a web interface.
